<h4>New Comment</h4>
<%= form_with(model:[post, comment], remote: true) do |f| %>
  <%= f.text_area :comment, rows:'3', id: 'textarea', class: 'form-control mt-3' %>
  <div class="text-right mt-2">
    <%= f.submit 'COMMENT', class: "comment-button button" %>
  </div>
<% end %>
