<h1>会員詳細</h1>

<table>
  <tr>
    <th>ID</th>
    <th>画像</th>
    <th>名前</th>
    <th>ユーザネーム</th>
    <th>メールアドレス</th>
    <th>電話番号</th>
    <th>会員ステータス</th>
  </tr>

  <tr>
    <td><%= @user.id %></td>
    <td><%= attachment_image_tag @user, :image, :fill, 50, 50, fallback: "no_image.jpg" %>
  </td>
    <td><%= @user.name %></td>
    <td><%= @user.user_name %></td>
    <td><%= @user.email %></td>
    <td><%= @user.telephone_number %></td>
    <td>
      <% if @user.is_deleted? %>
        退会
      <% else %>
        有効
      <% end %>
    </td>
  </tr>

</table>

<%= link_to '編集', edit_admin_user_path(@user.id) %>